<Window x:Class="KPWrestlingScoreboard.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Авторизация"
        Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="#0f0f0f" CornerRadius="15" BorderBrush="#2a2a2a" BorderThickness="1">
        <Grid>
            <!-- Кнопка закрытия -->
            <Button Content="✕" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top"
                    Margin="0,15,15,0"
                    Width="30" Height="30"
                    Background="Transparent"
                    Foreground="#666"
                    BorderThickness="0"
                    FontSize="14"
                    Cursor="Hand"
                    Click="Close_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#ff4444"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <StackPanel VerticalAlignment="Center" Margin="50,0">
                <!-- Логотип -->
                <Border Width="80" Height="80" CornerRadius="40" Background="#1a1a1a" Margin="0,0,0,25" HorizontalAlignment="Center">
                    <TextBlock Text="🤼" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                
                <!-- Заголовок -->
                <TextBlock Text="Добро пожаловать" 
                           FontSize="24" 
                           FontWeight="SemiBold" 
                           Foreground="White" 
                           HorizontalAlignment="Center"
                           FontFamily="Segoe UI"/>
                <TextBlock Text="Федерация спортивной борьбы" 
                           FontSize="12" 
                           Foreground="#666" 
                           HorizontalAlignment="Center"
                           Margin="0,5,0,35"
                           FontFamily="Segoe UI"/>
                
                <!-- Поле логина -->
                <TextBlock Text="ЛОГИН" Foreground="#555" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,8" FontFamily="Segoe UI"/>
                <Border Background="#1a1a1a" CornerRadius="8" Margin="0,0,0,20">
                    <TextBox x:Name="loginTextBox" 
                             Background="Transparent"
                             Foreground="White"
                             BorderThickness="0"
                             Padding="15,12"
                             FontSize="14"
                             CaretBrush="White"
                             FontFamily="Segoe UI"/>
                </Border>
                
                <!-- Поле пароля -->
                <TextBlock Text="ПАРОЛЬ" Foreground="#555" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,8" FontFamily="Segoe UI"/>
                <Border Background="#1a1a1a" CornerRadius="8" Margin="0,0,0,15">
                    <PasswordBox x:Name="passwordPasswordBox" 
                                 Background="Transparent"
                                 Foreground="White"
                                 BorderThickness="0"
                                 Padding="15,12"
                                 FontSize="14"
                                 CaretBrush="White"
                                 FontFamily="Segoe UI"/>
                </Border>
                
                <!-- Сообщение об ошибке -->
                <TextBlock x:Name="errorTextBlock" 
                           Foreground="#ff4444" 
                           FontSize="12" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           FontFamily="Segoe UI"/>
                
                <!-- Кнопка входа -->
                <Button Content="Войти в систему" 
                        Height="45"
                        Cursor="Hand"
                        Click="LoginButton_Click"
                        FontFamily="Segoe UI"
                        FontSize="14"
                        FontWeight="SemiBold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#DC2626"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" 
                                                Background="{TemplateBinding Background}" 
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#EF4444"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#B91C1C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Разделитель -->
                <Grid Margin="0,20,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Height="1" Background="#333" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="или" Foreground="#555" Margin="15,0" FontSize="12"/>
                    <Border Grid.Column="2" Height="1" Background="#333" VerticalAlignment="Center"/>
                </Grid>
                
                <!-- Кнопка входа как гость -->
                <Button Content="Войти как гость" 
                        Height="45"
                        Cursor="Hand"
                        Click="GuestLogin_Click"
                        FontFamily="Segoe UI"
                        FontSize="14"
                        FontWeight="SemiBold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#1a1a1a"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" 
                                                Background="{TemplateBinding Background}" 
                                                CornerRadius="8"
                                                BorderBrush="#333"
                                                BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#2a2a2a"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#333"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
