<Window x:Class="KPWrestlingScoreboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Федерация спортивной борьбы" 
        Height="800" Width="950"
        MinHeight="700" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#0a0a0a">
    
    <Window.Resources>
        <!-- Стиль для кнопок очков -->
        <Style x:Key="ScoreButton" TargetType="Button">
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3a3a3a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для основных кнопок -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для мини-кнопок -->
        <Style x:Key="MiniButton" TargetType="Button">
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для ComboBox Toggle Button -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" Background="#1a1a1a" CornerRadius="6"/>
                <Border Grid.Column="0" Background="Transparent" CornerRadius="6,0,0,6"/>
                <Path x:Name="Arrow" Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="Transparent"/>
        </ControlTemplate>

        <!-- Стиль для ComboBox -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}"
                                          Grid.Column="2" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,3,30,3" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}"
                                     Template="{StaticResource ComboBoxTextBox}"
                                     HorizontalAlignment="Left" VerticalAlignment="Center"
                                     Margin="10,3,30,3" Focusable="True" Background="Transparent"
                                     Foreground="White" Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#2a2a2a" CornerRadius="6" Margin="0,2,0,0"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ComboBoxItem -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Padding="{TemplateBinding Padding}" 
                                Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3a3a3a"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#DC2626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок и меню -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🤼" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Федерация спортивной борьбы" 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="База данных" Style="{StaticResource ActionButton}" Margin="5,0" Click="Database_Click"/>
                <Button x:Name="usersButton" Content="Пользователи" Style="{StaticResource ActionButton}" Margin="5,0" Click="Users_Click" Visibility="Collapsed"/>
                <Button Content="Справка" Style="{StaticResource ActionButton}" Margin="5,0" Click="About_Click"/>
                <Button Content="Выход" Style="{StaticResource ActionButton}" Click="Exit_Click" Background="#DC2626"/>
            </StackPanel>
        </Grid>
        
        <!-- Верхняя панель: Углы и категория -->
        <Grid x:Name="judgingRow1" Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Красный угол -->
            <Border Grid.Column="0" Background="#1a1a1a" CornerRadius="10" Padding="15">
                <StackPanel>
                    <Border Background="#DC2626" CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                        <TextBlock Text="КРАСНЫЙ УГОЛ" Foreground="White" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" FontFamily="Segoe UI"/>
                    </Border>
                    <ComboBox x:Name="redWrestlerComboBox" Style="{StaticResource DarkComboBox}" Margin="0,0,0,10"/>
                    <Button Content="Объявить победителя" Style="{StaticResource ActionButton}" Click="DeclareRedWinner_Click" Background="#DC2626" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
            
            <!-- Центр: Категория и период -->
            <Border Grid.Column="1" Background="#1a1a1a" CornerRadius="10" Padding="15" Margin="10,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="ВЕСОВАЯ КАТЕГОРИЯ" Foreground="#666" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,5" FontFamily="Segoe UI"/>
                    <ComboBox x:Name="weightCategoryComboBox" Style="{StaticResource DarkComboBox}" Width="100" HorizontalAlignment="Center" Margin="0,0,0,15" SelectionChanged="WeightCategoryComboBox_SelectionChanged"/>
                    <Border Background="#2a2a2a" CornerRadius="6" Padding="15,10">
                        <TextBlock x:Name="periodDisplayTextBlock" Text="ПЕРИОД 1" Foreground="White" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" FontFamily="Segoe UI"/>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- Синий угол -->
            <Border Grid.Column="2" Background="#1a1a1a" CornerRadius="10" Padding="15">
                <StackPanel>
                    <Border Background="#2563EB" CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                        <TextBlock Text="СИНИЙ УГОЛ" Foreground="White" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" FontFamily="Segoe UI"/>
                    </Border>
                    <ComboBox x:Name="blueWrestlerComboBox" Style="{StaticResource DarkComboBox}" Margin="0,0,0,10"/>
                    <Button Content="Объявить победителя" Style="{StaticResource ActionButton}" Click="DeclareBlueWinner_Click" Background="#2563EB" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Секция очков и главного таймера -->
        <Border x:Name="judgingRow2" Grid.Row="2" Background="#1a1a1a" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Красная сторона -->
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <Button Content="+1" Style="{StaticResource ScoreButton}" Click="RedPlus1_Click"/>
                        <Button Content="+2" Style="{StaticResource ScoreButton}" Click="RedPlus2_Click"/>
                        <Button Content="+3" Style="{StaticResource ScoreButton}" Click="RedPlus3_Click"/>
                        <Button Content="+4" Style="{StaticResource ScoreButton}" Click="RedPlus4_Click"/>
                        <Button Content="+5" Style="{StaticResource ScoreButton}" Click="RedPlus5_Click"/>
                    </StackPanel>
                    
                    <Border Background="#DC2626" CornerRadius="10" Padding="20,10" HorizontalAlignment="Center">
                        <TextBlock x:Name="redScoreTextBlock" Text="0" Foreground="White" FontSize="72" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button Content="-1" Style="{StaticResource ScoreButton}" Click="RedMinus1_Click"/>
                        <Button Content="-2" Style="{StaticResource ScoreButton}" Click="RedMinus2_Click"/>
                        <Button Content="-3" Style="{StaticResource ScoreButton}" Click="RedMinus3_Click"/>
                        <Button Content="-4" Style="{StaticResource ScoreButton}" Click="RedMinus4_Click"/>
                        <Button Content="-5" Style="{StaticResource ScoreButton}" Click="RedMinus5_Click"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Центр: Таймер -->
                <StackPanel Grid.Column="1" Margin="30,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <Button x:Name="period1Button" Content="П1" Style="{StaticResource MiniButton}" Background="#DC2626" Click="Period1_Click" Width="50"/>
                        <Button x:Name="period2Button" Content="П2" Style="{StaticResource MiniButton}" Click="Period2_Click" Width="50" Margin="5,0,0,0"/>
                    </StackPanel>
                    
                    <Border Background="#2a2a2a" CornerRadius="10" Padding="25,15">
                        <StackPanel>
                            <TextBlock x:Name="mainTimerTextBlock" Text="6:00" Foreground="#FFD700" FontSize="64" FontWeight="Bold" FontFamily="Consolas" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button Content="−" Style="{StaticResource MiniButton}" Click="TimerMinus_Click"/>
                        <Button x:Name="startButton" Content="▶" Style="{StaticResource MiniButton}" Click="StartStop_Click" Background="#22C55E" Width="60" Margin="10,0"/>
                        <Button Content="+" Style="{StaticResource MiniButton}" Click="TimerPlus_Click"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button x:Name="breakButton" Content="ПЕРЕРЫВ 30 сек" Style="{StaticResource ActionButton}" Click="Break_Click" Background="#F59E0B" Margin="0,0,10,0"/>
                        <Button x:Name="signalButton" Content="🔔 СИГНАЛ" Style="{StaticResource ActionButton}" Click="Signal_Click" Background="#EF4444" FontSize="14"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Синяя сторона -->
                <StackPanel Grid.Column="2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <Button Content="+1" Style="{StaticResource ScoreButton}" Click="BluePlus1_Click"/>
                        <Button Content="+2" Style="{StaticResource ScoreButton}" Click="BluePlus2_Click"/>
                        <Button Content="+3" Style="{StaticResource ScoreButton}" Click="BluePlus3_Click"/>
                        <Button Content="+4" Style="{StaticResource ScoreButton}" Click="BluePlus4_Click"/>
                        <Button Content="+5" Style="{StaticResource ScoreButton}" Click="BluePlus5_Click"/>
                    </StackPanel>
                    
                    <Border Background="#2563EB" CornerRadius="10" Padding="20,10" HorizontalAlignment="Center">
                        <TextBlock x:Name="blueScoreTextBlock" Text="0" Foreground="White" FontSize="72" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button Content="-1" Style="{StaticResource ScoreButton}" Click="BlueMinus1_Click"/>
                        <Button Content="-2" Style="{StaticResource ScoreButton}" Click="BlueMinus2_Click"/>
                        <Button Content="-3" Style="{StaticResource ScoreButton}" Click="BlueMinus3_Click"/>
                        <Button Content="-4" Style="{StaticResource ScoreButton}" Click="BlueMinus4_Click"/>
                        <Button Content="-5" Style="{StaticResource ScoreButton}" Click="BlueMinus5_Click"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Штрафы и индивидуальные таймеры -->
        <Grid x:Name="judgingRow3" Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Красный таймер и штраф -->
            <Border Grid.Column="0" Background="#1a1a1a" CornerRadius="10" Padding="15" Margin="0,0,7,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button Content="X" Style="{StaticResource MiniButton}" Background="#F59E0B" Foreground="Black" Click="RedPenalty_Click"/>
                        <ComboBox x:Name="redPenaltyComboBox" Style="{StaticResource DarkComboBox}" Width="50" Margin="5,0">
                            <ComboBoxItem Content="0" IsSelected="True"/>
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="2"/>
                            <ComboBoxItem Content="3"/>
                        </ComboBox>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="−" Style="{StaticResource MiniButton}" Click="RedTimerMinus_Click"/>
                        <TextBlock x:Name="redTimerTextBlock" Text="0:30" Foreground="White" FontSize="28" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center" Margin="10,0"/>
                        <Button Content="+" Style="{StaticResource MiniButton}" Click="RedTimerPlus_Click"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="▶" Style="{StaticResource MiniButton}" Background="#22C55E" Click="RedTimerStart_Click" Margin="0,0,3,0"/>
                        <Button Content="■" Style="{StaticResource MiniButton}" Background="#EF4444" Click="RedTimerStop_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Синий таймер и штраф -->
            <Border Grid.Column="1" Background="#1a1a1a" CornerRadius="10" Padding="15" Margin="7,0,0,0">
    <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button Content="▶" Style="{StaticResource MiniButton}" Background="#22C55E" Click="BlueTimerStart_Click" Margin="0,0,3,0"/>
                        <Button Content="■" Style="{StaticResource MiniButton}" Background="#EF4444" Click="BlueTimerStop_Click"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="−" Style="{StaticResource MiniButton}" Click="BlueTimerMinus_Click"/>
                        <TextBlock x:Name="blueTimerTextBlock" Text="0:30" Foreground="White" FontSize="28" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center" Margin="10,0"/>
                        <Button Content="+" Style="{StaticResource MiniButton}" Click="BlueTimerPlus_Click"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <ComboBox x:Name="bluePenaltyComboBox" Style="{StaticResource DarkComboBox}" Width="50" Margin="0,0,5,0">
                            <ComboBoxItem Content="0" IsSelected="True"/>
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="2"/>
                            <ComboBoxItem Content="3"/>
                        </ComboBox>
                        <Button Content="X" Style="{StaticResource MiniButton}" Background="#F59E0B" Foreground="Black" Click="BluePenalty_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Стадия соревнований -->
        <Border x:Name="judgingRow4" Grid.Row="4" Background="#1a1a1a" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="СТАДИЯ СОРЕВНОВАНИЙ" Foreground="#666" FontSize="10" Margin="0,0,0,10" FontFamily="Segoe UI"/>
                <WrapPanel>
                    <RadioButton x:Name="qualificationRadioButton" Content="QUALIFICATION" Foreground="White" Margin="0,0,20,5" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                    <RadioButton x:Name="round16RadioButton" Content="1/16 FINAL" Foreground="White" Margin="0,0,20,5" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                    <RadioButton x:Name="round8RadioButton" Content="1/8 FINAL" Foreground="White" Margin="0,0,20,5" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                    <RadioButton x:Name="quarterFinalRadioButton" Content="1/4 FINAL" Foreground="White" Margin="0,0,20,5" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                    <RadioButton x:Name="semiFinalRadioButton" Content="1/2 FINAL" Foreground="White" Margin="0,0,20,5" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                    <RadioButton x:Name="repechageRadioButton" Content="REPECHAGE" Foreground="White" Margin="0,0,20,5" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                    <RadioButton x:Name="bronzeFinalRadioButton" Content="FINAL 3-5" Foreground="White" Margin="0,0,20,5" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                    <RadioButton x:Name="goldFinalRadioButton" Content="FINAL 1-2" Foreground="White" IsChecked="True" FontFamily="Segoe UI" Checked="Stage_Changed"/>
                </WrapPanel>
            </StackPanel>
        </Border>
        
        <!-- Кнопка табло -->
        <StackPanel x:Name="judgingRow5" Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,15">
            <Button x:Name="toggleScoreboardButton" Content="Показать табло" Style="{StaticResource ActionButton}" Click="ToggleScoreboard_Click" Background="#8B5CF6" Padding="25,12"/>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Window>
